"""Skill artifact handling: schema validation and markdown (de)serialization.

Implements ``SchemaValid`` / ``BudgetValid`` / ``InvariantValid`` (membership in
the constrained space Z) and the ``SkillSerializer`` protocol bridging a typed
``SkillArtifact`` to/from SkillOpt's markdown skill string.

Planned modules: ``schema.py``, ``serializer.py``.
"""
